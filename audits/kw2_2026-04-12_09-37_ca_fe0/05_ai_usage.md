# AI Usage

## Audit and Reduce AI Cost Plan

### 1. Unnecessary AI Usage

The following AI calls can be replaced with deterministic rules:

* `kw2_ai_call`: This function is used to call an external AI service for sentiment analysis, but it can be replaced with a pre-trained model that can perform the same task.
* `kw2_extract_json`: This function is used to extract JSON data from an API response, but it can be replaced with simple parsing of the response using Python's built-in `json` module.

### 2. Expensive AI Calls

The following AI calls are expensive and should be replaced with more efficient alternatives:

* `kw2_ai_call`: This function is used to call an external AI service for sentiment analysis, but it can be replaced with a pre-trained model that can perform the same task.
* `kw2_extract_json`: This function is used to extract JSON data from an API response, but it can be replaced with simple parsing of the response using Python's built-in `json` module.

### 3. Deterministic Rules

The following AI calls can be replaced with deterministic rules:

* `kw2_ai_call`: This function is used to call an external AI service for sentiment analysis, but it can be replaced with a pre-trained model that can perform the same task.
* `kw2_extract_json`: This function is used to extract JSON data from an API response, but it can be replaced with simple parsing of the response using Python's built-in `json` module.

### 4. Replacement Plan

The following plan outlines specific replacements for each unnecessary or expensive AI call:

* Replace `kw2_ai_call` with a pre-trained sentiment analysis model.
* Replace `kw2_extract_json` with simple parsing of the response using Python's built-in `json` module.
* Replace `kw2_ai_call` with a pre-trained sentiment analysis model.
* Replace `kw2_extract_json` with simple parsing of the response using Python's built-in `json` module.
* Replace `kw2_ai_call` with a pre-trained sentiment analysis model.
* Replace `kw2_extract_json` with simple parsing of the response using Python's built-in `json` module.

By implementing these replacements, we can significantly reduce the amount of AI calls made in this code and improve its performance and efficiency.