# Defines the ordered steps for strategy pipeline execution.
STEPS = ["input", "keyword", "serp", "strategy", "linking", "scoring"]

STEP_PROGRESS = {
    "input": 5,
    "keyword": 20,
    "serp": 40,
    "strategy": 80,
    "linking": 90,
    "scoring": 100,
}
